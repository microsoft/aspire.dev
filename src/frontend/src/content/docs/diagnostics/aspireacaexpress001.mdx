---
title: Compiler Error ASPIREACAEXPRESS001
seoTitle: "ASPIREACAEXPRESS001: AsExpress is for evaluation"
description: Learn what causes the Aspire compiler error ASPIREACAEXPRESS001 and how to fix it so your AppHost builds cleanly.
---

import { Badge } from '@astrojs/starlight/components';

<Badge
  text="Version introduced: 13.6"
  variant="note"
  size="large"
  class:list={'mb-1'}
/>

> AsExpress is for evaluation purposes only and is subject to change or removal in future updates. Suppress this diagnostic to proceed.

## Example

The following code generates `ASPIREACAEXPRESS001`:

```csharp title="AppHost.cs"
var builder = DistributedApplication.CreateBuilder(args);

var env = builder.AddAzureContainerAppEnvironment("aspire-env")
    .AsExpress();
```

## To correct this error

Suppress the error with either of the following methods:

- Set the severity of the rule in the _.editorconfig_ file.

  ```ini title=".editorconfig"
  [*.{cs,vb}]
  dotnet_diagnostic.ASPIREACAEXPRESS001.severity = none
  ```

  For more information about editor config files, see [Configuration files for code analysis rules](/diagnostics/overview/#suppress-in-the-editorconfig-file).

- Add the following `PropertyGroup` to your project file:

  ```xml title="C# project file"
  <PropertyGroup>
      <NoWarn>$(NoWarn);ASPIREACAEXPRESS001</NoWarn>
  </PropertyGroup>
  ```

- Suppress in code with the `#pragma warning disable ASPIREACAEXPRESS001` directive.
