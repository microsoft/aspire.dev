---
title: Option List Steps
---

**`--list-steps`**

List the pipeline steps that would be executed, without running lifecycle callbacks or pipeline steps. Useful for inspecting what a pipeline command will do before invoking it for real.

**`--format <table|json>`**

Set the output format for `--list-steps`. Only valid together with `--list-steps`; the default is `table`.
