---
interface NextStep {
  number: string;
  title: string;
  description: string;
  link: string;
  linkText: string;
}

interface Props {
  steps: NextStep[];
}

const { steps } = Astro.props;
---

<div class="next-steps">
  <h2>🎯 Ready to Build for Real?</h2>
  <div class="next-steps-content">
    <p>
      The Aspire Playground is great for learning and prototyping, but the real power comes 
      from building actual applications. Here's how to take the next step:
    </p>
    
    <div class="steps-list">
      {steps.map((step) => (
        <div class="step-item">
          <strong>{step.number}. {step.title}</strong>
          <p set:html={step.description}></p>
          <a href={step.link} class="step-link">{step.linkText} →</a>
        </div>
      ))}
    </div>
  </div>
</div>

<style>
  .next-steps {
    max-width: 1200px;
    margin: 0 auto;
    padding: 3rem 1rem;
    background: var(--sl-color-bg-nav);
    border-radius: 12px;
  }

  .next-steps h2 {
    text-align: center;
    font-size: 2rem;
    margin-bottom: 1.5rem;
    color: var(--sl-color-white);
  }

  .next-steps-content > p {
    text-align: center;
    font-size: 1.125rem;
    color: var(--sl-color-gray-2);
    max-width: 800px;
    margin: 0 auto 2rem;
    line-height: 1.6;
  }

  .steps-list {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
    margin-top: 2rem;
  }

  .step-item {
    background: var(--sl-color-bg);
    border: 1px solid var(--sl-color-gray-5);
    border-radius: 8px;
    padding: 1.5rem;
  }

  .step-item strong {
    display: block;
    color: var(--sl-color-white);
    font-size: 1.125rem;
    margin-bottom: 0.5rem;
  }

  .step-item p {
    color: var(--sl-color-gray-3);
    margin: 0 0 1rem 0;
    line-height: 1.5;
  }

  .step-item :global(code) {
    background: var(--sl-color-gray-6);
    padding: 0.125rem 0.375rem;
    border-radius: 3px;
    font-family: var(--sl-font-mono);
    font-size: 0.875rem;
  }

  .step-link {
    color: var(--sl-color-accent);
    text-decoration: none;
    font-weight: 600;
    font-size: 0.9375rem;
    transition: opacity 0.2s;
  }

  .step-link:hover {
    opacity: 0.8;
    text-decoration: underline;
  }
</style>
